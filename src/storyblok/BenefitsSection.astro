---
import { storyblokEditable } from '@storyblok/astro'
import BenefitsSection from '../components/home-page/benefits-section/BenefitsSection.astro'

const { blok } = Astro.props
---

<div {...storyblokEditable(blok)}>
  <BenefitsSection {...blok} />
</div>
